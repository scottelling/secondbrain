// TODO: Session 4 — Stripe checkout session creator
// Creates a Stripe Checkout session for Pro plan subscription

export default function handler(req, res) {
  res.status(501).json({ error: 'Not implemented — Session 4' });
}
