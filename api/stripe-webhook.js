// TODO: Session 4 — Stripe webhook handler
// Updates user plan in Supabase on payment success

export default function handler(req, res) {
  res.status(501).json({ error: 'Not implemented — Session 4' });
}
